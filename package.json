{
  "name": "earplug-backend",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "backfill:release": "node tool/run_release_backfills.mjs",
    "check:release-contract": "node tool/check_convex_contract.mjs",
    "dev": "convex dev",
    "deploy": "convex deploy",
    "test": "vitest run --exclude 'tool/**' && node --test tool/*.test.mjs"
  },
  "dependencies": {
    "@clerk/backend": "^3.15.0",
    "@convex-dev/migrations": "^0.3.6",
    "convex": "^1.42.3"
  },
  "devDependencies": {
    "@edge-runtime/vm": "^5.0.0",
    "@types/node": "^26.1.2",
    "convex-test": "^0.0.54",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
